<section class="inventory-table common-goods">
  <h3>
    Common Goods
  </h3>

  <table class="item-table" data-table="commonGoods">
    <thead>
      <tr>
        <th>
          Name
        </th>
        <th>
          Weight
        </th>
        <th>
          Cost
        </th>
        <th class="weapon-actions-header">
          <a class="add-item" data-tooltip="Add Custom Item">
            <i class="fas fa-plus-circle"></i>
          </a>
        </th>
      </tr>
    </thead>

    <tbody>
      {{#each system.items.commonGoods as |item|}}
        <tr data-item-id="{{item.id}}" class="item-row">
          <td class="item-name item-edit clickable">
            {{item.name}}
          </td>
          <td>
            {{item.system.weight}}
          </td>
          <td>
            {{item.system.cost.value}} {{item.system.cost.type}}
          </td>
          <td class="item-controls">
            <a class="item-delete">
              <i class="fas fa-trash"></i>
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>