export function InitalizeAllActorData(actor) {
  //This handles calling all the functions that inialize all the system editable only fields for each object defined if any.
  const system = actor.system;
}
